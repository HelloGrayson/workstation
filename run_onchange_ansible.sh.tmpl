#!/bin/bash

# .bootstrap/ansible-playbook.yml hash: {{ include "dot_bootstrap/ansible-playbook.yml" | sha256sum }}

if command -v ansible-playbook &> /dev/null; then
    ansible-playbook {{ joinPath .chezmoi.sourceDir "dot_bootstrap/ansible-playbook.yml" | quote }} --ask-become-pass
fi

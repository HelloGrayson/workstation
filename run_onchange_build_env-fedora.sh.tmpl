#!/usr/bin/bash
set -xeuo pipefail

# src/Containerfile.env-fedora hash: {{ include "src/Containerfile.env-fedora" | sha256sum }}

main() {
  SRC="$(chezmoi data | jq .chezmoi.sourceDir -r)/src"

  bash "$SRC/build-env-fedora.sh"
}

main "${@}"

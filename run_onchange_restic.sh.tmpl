#!/bin/bash

# when ready to make a snapshot:
# restic backup --verbose --files-from=whitelist.txt

# .bootstrap/restic-includes.txt hash: {{ include "dot_bootstrap/restic-includes.txt" | sha256sum }}

# restore everything in restic.txt
# restic restore --verbose --exclude user-dirs.dirs --target ~ latest

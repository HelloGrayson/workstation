[Unit]
Description=Periodic Desktop Snapshot

[Service]
Type=oneshot
RemainAfterExit=yes
ExecStart=bash "$(chezmoi data | jq .chezmoi.sourceDir -r)/src/desktop-snapshot.sh" -v 2>&1 | logger

[Install]
WantedBy=default.target
